import React from 'react'
import cls from '../Account.module.scss'

const Reports = (props: Props) => {
    return (
        <div className={cls.module_wrapper}>
            <span className={cls.selected_module_title}>Отчеты</span>
        </div>
    )
}

export default Reports